mixin resent(img, subtitle, description)
    .middle-right
        .middle-right__img
            img(src='./img/' + img , alt= title)
        h3.middle-right__subtitle
            | #{subtitle}
        .middle-right__description
            | #{description}